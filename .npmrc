# Forcer les dépendances cross-platform
target_platform=linux
target_arch=x64
target_libc=libc

# Éviter les binaires spécifiques
optional=false

# Cache optimisé pour CI/CD
cache=/tmp/npm-cache

# Vérifier les plateformes
engine_strict=true
